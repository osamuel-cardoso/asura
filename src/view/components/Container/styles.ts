import styled from "styled-components"

export const ContainerStyle = styled.div`
    width: 100%;
    max-width: 90rem;
    padding: 0 1.25rem;
    margin: 0 auto;

    @media (max-width: 540px) {
        padding: 0 0.75rem;
    }
`
